[project]
name = "cyber-risk-intelligence-dashboard"
version = "1.0.0"
description = "Cyber Risk Intelligence Dashboard"

[tool.vercel]
builder = "python"

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project.scripts]
app = "app:app"
