{
  "installCommand": "pip install --upgrade pip && pip install -r requirements.txt",
  "buildCommand": "echo 'Build complete'",
  "devCommand": "uvicorn app:app --reload --port 3000",
  "framework": "python",
  "regions": ["iad1"]
}
